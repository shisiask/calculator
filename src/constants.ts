/**
 * Mathematical constants used throughout the calculator
 * Following IEEE 754 standards for floating-point arithmetic
 */

// Fundamental mathematical constants
export const PI = Math.PI;
export const E = Math.E;
export const SQRT2 = Math.SQRT2;
export const SQRT1_2 = Math.SQRT1_2;
export const LN2 = Math.LN2;
export const LN10 = Math.LN10;
export const LOG2E = Math.LOG2E;
export const LOG10E = Math.LOG10E;

// Golden ratio
export const PHI = (1 + Math.sqrt(5)) / 2;

// Precision constants
export const EPSILON = Number.EPSILON;
export const MAX_SAFE_INTEGER = Number.MAX_SAFE_INTEGER;
export const MIN_SAFE_INTEGER = Number.MIN_SAFE_INTEGER;
export const MAX_VALUE = Number.MAX_VALUE;
export const MIN_VALUE = Number.MIN_VALUE;

// Calculation limits
export const MAX_FACTORIAL = 170; // factorial(171) causes overflow
export const PRECISION_DIGITS = 15; // JavaScript number precision
